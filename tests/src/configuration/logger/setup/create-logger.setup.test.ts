/**
 * TODO: implement tests for createLoggerSetup
 *     - issue: https://github.com/matheusicaro/mi-node-framework/issues/8
 */
describe('createLoggerSetup', () => {
  test('should create the log files correctly before creating the instance', () => {});

  test('should create the logger instance correctly', () => {
    // expect(createLogger).toHaveBeenCalledWith()
  });

  test('should register the text in the log file correctly when instance is called', () => {});
});
