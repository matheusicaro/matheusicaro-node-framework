/**
 * TODO: implement tests for DependencyRegistry
 *     - issue: https://github.com/matheusicaro/mi-node-framework/issues/6
 */
describe('registerConfigs', () => {
  test('should register instances correctly', () => {});

  test('should register logger instance as a singleton correctly', () => {});
});
